@model poc1.Model.VacationList

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<div>
    @Model.DiasTomar.Count
    <h4>VacationList</h4>
    <hr />
    @foreach (var item in Model.DiasTomar)
    {
        <div class="row">
            <div class="col-md-2">
                @item.Name

                @foreach (var dias in item.Dias)
                {
                    
                            @Html.DisplayFor(modelItem => dias.Day)
                      <br />
                }
            </div>

            </div>
            }
</div>
<div>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    <a asp-action="Index">Back to List</a>
</div>
